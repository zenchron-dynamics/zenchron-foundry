# Zenchron Foundry — Release War Room · v2026.07.02

Phase-0 coordination record. Owner: Developer 1 (Release Commander) · Audit: Developer 10.

## Release identity

| Field | Value |
|---|---|
| Version (CalVer) | `v2026.07.02` |
| Candidate | `rc1` |
| Intended release commit | `3b34157c575a0950b20a4f83d97e4b3f0c83e38b` |
| Source ref | `refs/heads/master` |
| Prior stable (must NOT be overwritten) | `v2026.06.21` (tag `4e72f3c…`) |
| Platform | Debian-first, 10 images (PHP 8.3/8.4 × cli/fpm/worker/frankenphp + nginx + caddy) |

Master CI on the intended commit is **green**: run `28597652716` — 5 static gates + 10/10 build+smoke, all success (GitHub-hosted runners; 0 self-hosted online).

## Grounding snapshot (2026-07-02)

- Existing tags: `v2026.05.30`, `v2026.06.03`, `v2026.06.21`.
- No release workflow currently running; no self-hosted runner online (`actions/runners` total_count=0 → CI runs on GitHub-hosted).
- `cosign`/`crane` are **not installed on this operator host** → signature/provenance/digest verification cannot be performed locally, only inside CI. No trust artifact is fabricated in this document.

## Team ownership

| Dev | Area |
|---|---|
| 1 | Coordination, sequencing, stop/go, evidence, release report |
| 2 | RC identity + commit binding, release-tag/revision equality |
| 3 | Exact-digest stable promotion + compensating rollback |
| 4 | Workflows, permissions, environments, exact-commit CI checks |
| 5 | Runner hardening (`scripts/ci/reset-workspace-ownership.sh`), capacity |
| 6 | Signature/SBOM/provenance verification, registry post-publication checks |
| 7 | Trivy/Grype, Caddy CVE, exception policy + validator |
| 8 | 10-image cold builds, smoke, runtime contracts, multi-arch |
| 9 | Unsafe-helper removal, release scripts, docs, runbooks |
| 10 | Independent adversarial audit + final verdict |

## Alias inventory (what stable promotion will mutate)

Derived from `scripts/promote-stable.sh` (`aliases_for`). REL = `v2026.07.02`.

PHP family (each of 8.3/8.4 × cli/fpm/worker/frankenphp):
- `<ver>-prod`  ← canonical (mutated **last**)
- `<ver>-prod-v2026.07.02`  ← version-pinned
- `<ver>-debian`  ← provider-explicit

Edges (caddy, nginx):
- `prod`  ← canonical (mutated **last**)
- `prod-v2026.07.02`  ← version-pinned

Rollback = the pre-promotion digest of every canonical alias above, captured in Phase 1 of `promote-stable.sh` before any mutation.

## Release gates (all must be proven, none inferred)

`build 10/10 · smoke 10/10 · Trivy 10/10 policy-compliant · Grype 10/10 · signature 10/10 · SBOM 10/10 · provenance 10/10 · amd64 10/10 · arm64 10/10`

Plus identity equality for all 10:
`release-tag SHA == RC manifest revision == RC provenance revision == RC OCI label == stable provenance revision == stable OCI label`, and `stable digest == validated RC digest`.

## Stop conditions (defer stable promotion if ANY hold)

- Any required CI job not green, or exact release commit unverifiable.
- Any image missing; any provenance revision differs; any stable alias ≠ RC digest.
- Rollback metadata incomplete or rollback trap untested.
- Caddy accepted-risk exception unapproved (see `docs/security/cve-2026-34986-review.md`).
- `foundry-production` reviewers not configured (cannot be proven from repo code).
- Registry verification cannot run (`cosign`/`crane` unavailable in the executing environment).
- Any mandatory negative test skipped.

Safe deferral outcome: **`RC VALIDATED — STABLE RELEASE DEFERRED`** (a successful safe result, not a failure).

## Current blocking reality (2026-07-02)

The live back-half of the release (RC publish → live GHCR verify → stable promotion → rollback → GitHub release) is **not executable from this operator host today**: it is outward-facing/irreversible, gated behind the `foundry-production` protected environment (reviewers cannot be self-approved), and requires live digests/signatures/SBOM/provenance/scan results that must be produced by CI and MUST NOT be fabricated. Code and policy gates are complete and locally proven (Checkpoint A); live checkpoints D–F remain pending authorized CI execution + environment approval.
